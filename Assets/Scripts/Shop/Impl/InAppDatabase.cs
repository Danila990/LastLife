using Shop.Abstract;
using UnityEngine;
using Utils;

namespace Shop.Impl
{
	[CreateAssetMenu(menuName = SoNames.SHOP + nameof(InAppDatabase), fileName = nameof(InAppDatabase))]
	public class InAppDatabase : AInAppDatabase
	{

		
	}
}